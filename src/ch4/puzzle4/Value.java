package ch4.puzzle4;

class//static
Value
{    int intValue;//static

    public int
    doStuff(int//static
                    factor
    ) { if (intValue > 100) {//static
            return //static
                    intValue * factor;
        } else {//static
            return//static
                    intValue * (5 - factor);
        }
    }
}

